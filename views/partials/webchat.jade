h1 WebChat
div#chat-container
  div#chatbox
    p(ng-repeat='message in messages')
      &lt;{{message.nick}}&gt; {{message.to}} : {{message.input}}
  div#input-container
    input#input-box(ng-model="inputMsg", ng-enter="sendMsg()")
    button(ng-click="sendMsg()") Send